import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  image: {
    maxHeight: '500px',
    width: 'auto',
    borderRadius: '20px',
    objectFit: 'cover',
    boxShadow: '0.5em 0.5em 1em'
  }
}))